---
title: "Hermes Agent · 手动配置"
description: "Hermes Agent 的 config.yaml、.env、hermes model 与 custom provider 安全配置。"
navLabel: "手动配置"
---

# Hermes Agent · 手动配置

<VersionBadge version="v0.19.0 (tag v2026.7.20)" date="2026-07-20" license="MIT" github="https://github.com/NousResearch/hermes-agent" />

<PageHeader
  useCase="管理 Hermes 的 provider、模型、custom endpoint 与密钥"
  audience="需要自定义端点、切换 provider 或审计凭据位置的用户"
  tldr="hermes model 配 provider → 密钥进 ~/.hermes/.env → 非敏感设置进 config.yaml"
/>

---

## 当前配置结构

| 文件 | 用途 |
| --- | --- |
| `~/.hermes/config.yaml` | 模型、终端、压缩、记忆和工具等非敏感设置 |
| `~/.hermes/.env` | API Key、Token、密码等 secrets |
| `~/.hermes/auth.json` | OAuth provider 凭据 |

`HERMES_HOME` 可切换 Hermes profile 目录。不要改变系统 `HOME` 来模拟 profile；官方文档明确将 `HERMES_HOME` 与操作系统用户目录分开。

<Callout type="warning" title="密钥只放 secrets 存储">
Hermes 官方规则是 secrets 放 `.env`，非敏感设置放 `config.yaml`。`hermes config set <API_KEY_NAME> <value>` 会把 API Key 路由到 `.env`；这些文件都不能上传仓库、截图或在线校验站。
</Callout>

---

## 推荐：使用 provider 向导

```bash
# 在聊天外运行：添加 provider、OAuth、API Key 或 custom endpoint
hermes model

# 查看当前配置
hermes config

# 打开 config.yaml
hermes config edit

# 检查升级后缺失项
hermes config check
```

聊天内的 `/model` 只能切换已经配置好的 provider/model，不能新增 provider 或完成 OAuth。旧版页面把 `hermes config set provider.<name>...` 与 `api_mode` 写成所有 provider 的必填 schema；当前官方文档不支持这种概括，已撤下。

---

## OpenAI 兼容 custom provider

Hermes 官方说明 custom endpoint 应实现 `/v1/chat/completions`。最稳妥的方式是在 `hermes model` 中选择 **Custom endpoint**。如需命名多个 custom provider，可使用官方 `custom_providers` schema：

```yaml
custom_providers:
  - name: foropencode
    base_url: https://foropencode.com/v1
    key_env: FOROPENCODE_API_KEY

model:
  provider: custom:foropencode
  default: gateway-model-id
```

并把 Key 放到 `~/.hermes/.env`：

```dotenv
FOROPENCODE_API_KEY=sk-xxx
```

`foropencode.com` 是第三方网关，不是 Nous Research、OpenAI 或 Anthropic。`gateway-model-id` 必须替换为网关实际模型列表中的值。

<Callout type="warning" title="协议不能靠猜">
通用 custom provider 路径面向 OpenAI-compatible Chat Completions。若端点使用 Codex Responses、Anthropic Messages、Azure Foundry 或其他协议，应通过 `hermes model` 选择 Hermes 当前明确支持的 provider/模式，并同时核对端点发布方文档；不要把 `api_mode` 当成能让任意协议自动兼容的开关。
</Callout>

---

## 环境变量引用

`config.yaml` 支持 `${VAR_NAME}` 引用环境变量。密钥仍应放在 `.env`：

```yaml
auxiliary:
  vision:
    api_key: ${VISION_API_KEY}
    base_url: ${VISION_BASE_URL}
```

Hermes 的辅助模型可能处理视觉、网页摘要或其他工具任务。即使主聊天使用某个 provider，也要在 `hermes portal info`、`hermes tools` 和配置中核对辅助任务最终路由，避免误解数据流。

---

## 验证与切换

```bash
hermes config check
hermes model
hermes
```

provider 切换不要求重启，历史、记忆和 skills 会保留。切换前确认这些持久内容是否适合发送给新的 provider，并从不含敏感数据的最小请求开始。

<RelatedPages
  title="相关"
  items={[
    { route: '/hermes/cc-switch', hint: 'CC Switch 第三方配置边界' },
    { route: '/hermes/install', hint: '安装 Hermes Agent' },
    { route: '/site-info', hint: 'API Key、隐私与第三方关系' },
  ]}
/>

资料来源：[Hermes Configuration](https://hermes-agent.nousresearch.com/docs/user-guide/configuration) · [AI Providers](https://hermes-agent.nousresearch.com/docs/integrations/providers) · [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent)（2026-07-26 核对）
